PORT=3000
# Local dev uses these:
DB_HOST=localhost
DB_PORT=5433
DB_USER=med222
DB_PASS=
DB_NAME=royaa_db
# Production (Render/Neon) uses this instead — if set, it overrides the DB_* vars above:
# DATABASE_URL=postgres://user:pass@host/dbname?sslmode=require
DEFAULT_LANGUAGE=ar
SUPPORTED_LANGUAGES=ar,en
DEFAULT_CURRENCY=YER
JWT_SECRET=
JWT_EXPIRES_IN=7d
